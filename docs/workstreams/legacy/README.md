# Legacy Canonical Docs

These files were formerly top-level canonical docs.

They were moved here because the active docs set was rewritten to match the current codebase more accurately.

Use these only for historical comparison, not as source of truth.
